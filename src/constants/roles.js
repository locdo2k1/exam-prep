// User roles in the system
export const ROLES = {
   GUEST: 'guest',         // Unauthenticated users
   USER: 'user',           // Standard authenticated users
   EDITOR: 'editor',       // Users with content editing privileges
   ADMIN: 'admin',         // Administrators with full access
   SUPERVISOR: 'supervisor' // Supervisory role with elevated permissions
}